
import React from 'react';
import './Button.css';
import RoutesEnum from '../../Route/RoutesEnum';

const Button = (props: {routes: RoutesEnum, style: 'yellow'|'white'}) => (
    <h1>
        hellow
    </h1>
)

export default Button;